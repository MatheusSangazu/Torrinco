import { Link } from 'react-router-dom';
import { Moon, Sun, CreditCard, Download } from 'lucide-react';
import { useTheme } from '../context/ThemeContext';
import { useAuth } from '../context/AuthContext';
export function Settings(){const{theme,toggleTheme}=useTheme();const{user}=useAuth();const resetInstall=()=>{localStorage.removeItem('torrinco:pwa-install-dismissed');window.location.reload()};return <div className="mx-auto max-w-3xl space-y-6"><div><h1 className="text-3xl font-bold">Configurações</h1><p className="text-gray-500">Preferências da sua experiência no Torrinco.</p></div><section className="rounded-2xl bg-white p-6 shadow-sm dark:bg-dark-card"><h2 className="font-bold">Aparência</h2><button onClick={toggleTheme} className="mt-4 flex w-full items-center justify-between rounded-xl border p-4 text-left focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-torrinco-600"><span>{theme==='dark'?'Usando tema escuro':'Usando tema claro'}</span>{theme==='dark'?<Moon aria-hidden="true"/>:<Sun aria-hidden="true"/>}</button></section><section className="rounded-2xl bg-white p-6 shadow-sm dark:bg-dark-card"><h2 className="font-bold">Conta</h2><p className="mt-2 text-gray-600 dark:text-gray-300">{user?.name}</p><Link to="/subscription" className="mt-4 flex items-center gap-2 rounded-xl border p-4 font-semibold focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-torrinco-600"><CreditCard aria-hidden="true"/>Ver plano e limites</Link></section><section className="rounded-2xl bg-white p-6 shadow-sm dark:bg-dark-card"><h2 className="font-bold">Instalação</h2><p className="mt-2 text-sm text-gray-500">Reexiba o convite caso ele tenha sido dispensado neste dispositivo.</p><button onClick={resetInstall} className="mt-4 flex items-center gap-2 rounded-xl border px-4 py-3 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-torrinco-600"><Download aria-hidden="true"/>Reativar convite de instalação</button></section></div>}
